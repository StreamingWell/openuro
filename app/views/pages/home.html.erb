<div class="jumbotron">
	<h1>Astellas Oncology LIVE Lunchtime Webinars</h1>
	<h4><i>A multidisciplinary approach to prostate cancer understanding and management</i></h4>

	<br>
	<h4>To register for this webinar series please complete the following information:</h4>
	
	<div class="row">
	<div class="login span3">
<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: { class: 'form-horizontal'}) do |f| %>
    <%= f.input :name, :autofocus => true %>
    <%= f.input :email, :autofocus => true %>
    <%= f.input :company, :autofocus => true, label: "Place of Work" %>
    <%= f.input :job, :autofocus => true, label: "Job Title" %>
    <%= f.input :password %>
    <%= f.input :password_confirmation %>
    	<div class="span2">
        </div>
            <div class= "row">
                <div class="span6">
                    <p>Are you a healthcare professional? Please select either yes or no:</p>
                    <%= f.input :hcp, :as => :boolean, label: "Yes" %>
    <%= f.input :nothcp, :as => :boolean, label: "No" %>
    		<p>Please tick this box if you permit two reminder emails to be sent to you prior to each live webinar and also again once each webinar has been archived for viewing on the website.</p> <%= f.input :reminder, :as => :boolean, :label => "Accept", :input_html => { :checked => "checked" } %>
        </div>

    	</div>
    <div class="form-actions">
    <%= f.button :submit, "Register", class: 'btn btn-primary btn-large' %>
</div>
<% end %>
</div>
</div>
</div>

      <%= render 'layouts/prefooter' %>
